import { FC } from 'react';
declare type Props = {
    disableCache?: boolean;
    buttonText?: string;
    imageUrl: string;
};
declare const UrlImageDownloader: FC<Props>;
export default UrlImageDownloader;
